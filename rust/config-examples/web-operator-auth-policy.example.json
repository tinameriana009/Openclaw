{
  "policyKind": "claw.web-operator-auth-boundary",
  "schemaVersion": 1,
  "backendEnabled": false,
  "deploymentMode": "static-only",
  "anonymousReadAllowed": false,
  "mutationRoutesEnabled": false,
  "sessionCookiesSupported": false,
  "directInternetExposureAllowed": false,
  "trustedProxy": {
    "required": true,
    "identityHeaders": [
      "x-forwarded-user",
      "x-forwarded-email",
      "x-auth-request-id"
    ],
    "allowClientSuppliedIdentityHeaders": false
  },
  "localOperatorMutations": {
    "enabled": false,
    "requireLoopbackBind": true,
    "requiredAckHeader": "x-claw-local-operator"
  },
  "notes": [
    "This repo does not currently implement first-class web auth or browser session management.",
    "mutationRoutesEnabled stays false because there is still no authenticated web backend.",
    "If you temporarily opt into localOperatorMutations for localhost queue testing, keep the daemon loopback-bound and require the explicit acknowledgment header on every mutation request."
  ]
}
